[package]
name = "plato-correlator"
version = "0.1.0"
edition = "2021"
description = "Cross-sense fusion engine — fuses text shadows from multiple sense modules into unified events"

[dependencies]
uuid = { version = "1", features = ["v4"] }
